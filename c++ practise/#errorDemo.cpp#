#include <iostream>
#include <vector>

using std::vector;
using std::cin;
using std::cout;
using std::endl;

int main()
{
  vector<int> vec{0,1,2,3,4};

  for (auto it = vec.begin(); it != vec.end()/ && !it->empty(); ++it)
    cout << *it << ",";
  cout <<endl;

  return 0;
}
